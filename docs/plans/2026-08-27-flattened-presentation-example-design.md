# Flattened Presentation Example

The documentation gallery includes a `propertyDefinitions` example that promotes `lines[0]`, `note`, and `lines[1]` to top-level rows in that exact order. The raw `lines` array parent is absent from the presentation tree; each promoted item retains explicitly declared SKU and quantity children.
