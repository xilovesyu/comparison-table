# Array Index Labels

Numeric array-property labels include their parent display path, so immediate children of `lines` render as `lines[0]` and `lines[1]`. Internal paths remain arrays and are unchanged for matching, filtering, and expansion.
