# 自动 Diff 综合示例集成设计

“自动 Diff 与自定义比较”移动到“综合高级配置”之前。综合案例加入 `comparison` 配置：基准版本为 `baseline`；`billing.money.amount` 的差异采用 50 的金额容差；其余字段采用标准值比较。综合案例保留默认 `Diff` 标识，以展示直接变化和聚合数量。

测试验证示例顺序，并确认综合案例同时显示局部金额、内置金额和 Diff 标识。
