# Diff Badge 视觉优化设计

默认差异标识采用浅色填充与橙红描边的紧凑标签框：`Diff` 文本居中，具有固定最小高度、水平留白和细边框。聚合数量使用独立的实心圆形徽章，定位在标签右上方，与标签文字和边框保持间距。父级聚合行使用更浅的色调，直接变化行使用更高对比度的色调。

该调整不改变 `differenceIndicator` 的自定义 API；自定义节点继续完全控制自己的外观。
