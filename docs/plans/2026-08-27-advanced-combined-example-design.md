# Advanced Combined Example

The final documentation example combines controlled expansion, explicit presentation definitions, flattened array entries, path-level money renderers, custom labels and values, selection filtering, null/missing values, and global/local search. It excludes the sensitive customer secret while displaying a tree and flat rows together.
