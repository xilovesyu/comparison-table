# Summary Money Example

The basic comparison example contains two Money properties: an expandable money object and a summary-only money object. The summary property has a path rule with the money renderer and `expand: false`, so it is displayed as a single formatted cell per version without child rows or expansion controls.
