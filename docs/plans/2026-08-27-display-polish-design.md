# Display Polish

Path-level display rules may select a named renderer. The basic example assigns the money renderer to the money path, producing a monetary summary while retaining its recursive children. Styling improves hierarchy, compact actions, headers, and source-panel placement without changing the table's public behavior.
